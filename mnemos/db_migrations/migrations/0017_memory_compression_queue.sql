-- 0017_memory_compression_queue.sql — PostgreSQL numbered parity anchor.
-- The concrete current queue schema is db/migrations/0040_memory_compression_queue_parity.sql.
CREATE EXTENSION IF NOT EXISTS pgcrypto;
