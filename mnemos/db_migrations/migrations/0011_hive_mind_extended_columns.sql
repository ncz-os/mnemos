-- 0011_hive_mind_extended_columns.sql — PostgreSQL numbered parity anchor.
-- The PG numbered Hive Mind split (0021-0027) already includes the columns this
-- historical Oracle hotfix added.  Keep basename parity with an idempotent no-op.
SELECT 1;
