-- 0002_graeae.sql — PostgreSQL numbered parity anchor.
-- GRAEAE consultation/audit DDL is shipped by db/migrations.sql and
-- db/migrations_v3_graeae_unified.sql on PostgreSQL.  Keep this file
-- idempotent so all backend trees carry the historical basename.
CREATE EXTENSION IF NOT EXISTS pgcrypto;
